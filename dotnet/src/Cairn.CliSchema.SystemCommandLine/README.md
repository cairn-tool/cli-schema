# Cairn.CliSchema.SystemCommandLine

System.CommandLine 2.x emitter for the portable CLI-description schema. Depends on `Cairn.CliSchema` (the model) and System.CommandLine.

```csharp
using Cairn.CliSchema;
using Cairn.CliSchema.SystemCommandLine;

rootCommand.AddDescribeCommand(registry);
```
